package io.github.fg_project.combat.events;

public enum CommonEvent implements EventMarker {
    IDLE,
    WALKFORWARD,
    WALKBACK,
    CROUCH,
    JUMP,
    LAND,
    BLOCK,
    LIGHTATK,
    HIT,
    RECOVER,
    DIE
}
